<!-- author: Brett Bush -->

<!-- Employer Name Form Render -->
<%= form_tag("/report/show", method:"post", onsubmit:"report_form_reset()") do %>

  <h3>Report by Employer Name</h3>
  <p>Make a list of users by their employer name.</p>

  <%= label_tag(:employer_name, 'Employer name:') %>
  <%= select_tag(:employer_name, (options_from_collection_for_select(Company.all, :company_name, :company_name))) %>

  <%= label_tag(:saved_list_type, 'Run report on: ') %>
  <%= select_tag(:saved_list_type, (options_from_collection_for_select(SavedList.where("login_id = ?", current_login.id), :id, :list_name)), {:include_blank => 'All Users'}) %>

  <!-- Form identifiers -->
  <%= hidden_field_tag(:report_id, 4) %>
  <%= hidden_field_tag(:report_name, "Employer Name") %>

  <%= submit_tag('Submit', :class => 'button') %>
<% end %>
